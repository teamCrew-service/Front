import styled from 'styled-components';

const HeadLineParagraphStyle = styled.div`
  font-size: 20px;
  font-weight: 700;
  line-height: 28px;
  letter-spacing: -0.4px;
`;

export default HeadLineParagraphStyle;
