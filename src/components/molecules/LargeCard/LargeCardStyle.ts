import styled from 'styled-components';
import { Link } from 'react-router-dom';

const LargeCardStyle = styled(Link)`
  width: fit-content;
  height: fit-content;
  border-radius: 22px;
  text-decoration: none;
  color: black;
`;

export default LargeCardStyle;
